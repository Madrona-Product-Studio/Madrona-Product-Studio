export interface Article {
  title: string;
  description: string;
  date: string;
  url: string;
  source: string;
}

// Intentionally empty until real, published writing is available.
export const articles: Article[] = [];
